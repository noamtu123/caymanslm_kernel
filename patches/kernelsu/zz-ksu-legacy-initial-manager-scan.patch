diff --git a/kernel/runtime/boot_event.c b/kernel/runtime/boot_event.c
index 0000000..0000000 100644
--- a/kernel/runtime/boot_event.c
+++ b/kernel/runtime/boot_event.c
@@ -28,6 +28,8 @@ void on_post_fs_data(void)
 	pr_info("on_post_fs_data!\n");
 
 	ksu_load_allow_list();
 	ksu_observer_init();
+	/* The manager may already be installed before this boot. */
+	track_throne(false);
 	// sanity check, this may influence the performance
 	stop_input_hook();
 }
