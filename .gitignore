# Build output and upstream checkouts. Everything here is regenerable by
# scripts/setup-tree.sh + scripts/build.sh from the refs pinned in pins.sh.
artifacts/
build/
src/
third_party/
prebuilt/

# Editor / OS noise
*.swp
.DS_Store
Thumbs.db
.work/

# `>NUL 2>&1` in Windows-authored tooling creates a literal file here
NUL

# Wedge-trial logcat captures (tests/duck-wedge-trial.sh)
tests/wedge-runs/
