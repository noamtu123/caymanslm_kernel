diff --git a/kernel/events/core.c b/kernel/events/core.c
index 547184b71dce..a4574e87a339 100644
--- a/kernel/events/core.c
+++ b/kernel/events/core.c
@@ -6361,8 +6361,10 @@ perf_iterate_ctx(struct perf_event_context *ctx,
 		   void *data, bool all)
 {
 	struct perf_event *event;
+	int __lg_pic = 0;
 
 	list_for_each_entry_rcu(event, &ctx->event_list, event_entry) {
+		if (unlikely(++__lg_pic > 4000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: perf_iterate_ctx runaway\n"); break; }
 		if (!all) {
 			if (event->state < PERF_EVENT_STATE_INACTIVE)
 				continue;
@@ -6378,8 +6380,10 @@ static void perf_iterate_sb_cpu(perf_iterate_f output, void *data)
 {
 	struct pmu_event_list *pel = this_cpu_ptr(&pmu_sb_events);
 	struct perf_event *event;
+	int __lg_psc = 0;
 
 	list_for_each_entry_rcu(event, &pel->list, sb_list) {
+		if (unlikely(++__lg_psc > 4000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: perf_iterate_sb_cpu runaway\n"); break; }
 		/*
 		 * Skip events that are not fully formed yet; ensure that
 		 * if we observe event->ctx, both event and ctx will be
diff --git a/mm/mmap.c b/mm/mmap.c
index f59d8e681725..79488492f99e 100644
--- a/mm/mmap.c
+++ b/mm/mmap.c
@@ -521,12 +521,14 @@ static int find_vma_links(struct mm_struct *mm, unsigned long addr,
 		struct rb_node ***rb_link, struct rb_node **rb_parent)
 {
 	struct rb_node **__rb_link, *__rb_parent, *rb_prev;
+	int __lg_fvl = 0;
 
 	__rb_link = &mm->mm_rb.rb_node;
 	rb_prev = __rb_parent = NULL;
 
 	while (*__rb_link) {
 		struct vm_area_struct *vma_tmp;
+		if (unlikely(++__lg_fvl > 4000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: find_vma_links runaway\n"); break; }
 
 		__rb_parent = *__rb_link;
 		vma_tmp = rb_entry(__rb_parent, struct vm_area_struct, vm_rb);
@@ -1896,6 +1898,7 @@ unsigned long unmapped_area(struct vm_unmapped_area_info *info)
 	struct mm_struct *mm = current->mm;
 	struct vm_area_struct *vma;
 	unsigned long length, low_limit, high_limit, gap_start, gap_end;
+	int __lg_ua = 0;
 
 	/* Adjust search length to account for worst case alignment overhead */
 	length = info->length + info->align_mask;
@@ -1919,6 +1922,7 @@ unsigned long unmapped_area(struct vm_unmapped_area_info *info)
 		goto check_highest;
 
 	while (true) {
+		if (unlikely(++__lg_ua > 4000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: unmapped_area runaway\n"); break; }
 		/* Visit left subtree if it looks promising */
 		gap_end = vm_start_gap(vma);
 		if (gap_end >= low_limit && vma->vm_rb.rb_left) {
@@ -1991,6 +1995,7 @@ unsigned long unmapped_area_topdown(struct vm_unmapped_area_info *info)
 	struct mm_struct *mm = current->mm;
 	struct vm_area_struct *vma;
 	unsigned long length, low_limit, high_limit, gap_start, gap_end;
+	int __lg_ua = 0;
 
 	/* Adjust search length to account for worst case alignment overhead */
 	length = info->length + info->align_mask;
@@ -2023,6 +2028,7 @@ unsigned long unmapped_area_topdown(struct vm_unmapped_area_info *info)
 		return -ENOMEM;
 
 	while (true) {
+		if (unlikely(++__lg_ua > 4000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: unmapped_area_topdown runaway\n"); break; }
 		/* Visit right subtree if it looks promising */
 		gap_start = vma->vm_prev ? vm_end_gap(vma->vm_prev) : 0;
 		if (gap_start <= high_limit && vma->vm_rb.rb_right) {
diff --git a/security/selinux/avc.c b/security/selinux/avc.c
index 174c4abcda4a..c732785d6d72 100644
--- a/security/selinux/avc.c
+++ b/security/selinux/avc.c
@@ -34,6 +34,11 @@
 #include "avc_ss.h"
 #include "classmap.h"
 
+#ifdef CONFIG_KSU_SUSFS
+extern u32 susfs_ksu_sid;
+extern struct static_key_false susfs_is_avc_log_spoofing_enabled;
+#endif
+
 #define AVC_CACHE_SLOTS			512
 #define AVC_DEF_CACHE_THRESHOLD		512
 #define AVC_CACHE_RECLAIM		16
@@ -116,7 +121,19 @@ static void avc_dump_av(struct audit_buffer *ab, u16 tclass, u32 av)
 		return;
 	}
 
-	BUG_ON(!tclass || tclass >= ARRAY_SIZE(secclass_map));
+	/*
+	 * secclass_map is a COMPILE-TIME table, but KernelSU adds classes to the
+	 * live policydb, so tclass can legitimately exceed it here.  This runs in
+	 * the denial-audit path (enforcing only) under avc/policy locks, where a
+	 * BUG() is an unrecoverable panic an unprivileged app can trigger just by
+	 * causing the right denial.  Degrade the audit record instead.
+	 */
+	if (unlikely(!tclass || tclass >= ARRAY_SIZE(secclass_map))) {
+		pr_warn_once("SELinux: avc_dump_av: tclass %u outside secclass_map (%zu)\n",
+			     tclass, ARRAY_SIZE(secclass_map));
+		audit_log_format(ab, " av=0x%x", av);
+		return;
+	}
 	perms = secclass_map[tclass-1].perms;
 
 	audit_log_format(ab, " {");
@@ -157,15 +174,29 @@ static void avc_dump_query(struct audit_buffer *ab, u32 ssid, u32 tsid, u16 tcla
 		kfree(scontext);
 	}
 
-	rc = security_sid_to_context(tsid, &scontext, &scontext_len);
-	if (rc)
-		audit_log_format(ab, " tsid=%d", tsid);
-	else {
-		audit_log_format(ab, " tcontext=%s", scontext);
-		kfree(scontext);
+#ifdef CONFIG_KSU_SUSFS
+	if (static_branch_likely(&susfs_is_avc_log_spoofing_enabled) &&
+	    unlikely(tsid == susfs_ksu_sid)) {
+		audit_log_format(ab, " tcontext=%s",
+				 "u:r:priv_app:s0:c512,c768");
+	} else
+#endif
+	{
+		rc = security_sid_to_context(tsid, &scontext, &scontext_len);
+		if (rc)
+			audit_log_format(ab, " tsid=%d", tsid);
+		else {
+			audit_log_format(ab, " tcontext=%s", scontext);
+			kfree(scontext);
+		}
 	}
 
-	BUG_ON(!tclass || tclass >= ARRAY_SIZE(secclass_map));
+	if (unlikely(!tclass || tclass >= ARRAY_SIZE(secclass_map))) {
+		pr_warn_once("SELinux: avc_dump_query: tclass %u outside secclass_map (%zu)\n",
+			     tclass, ARRAY_SIZE(secclass_map));
+		audit_log_format(ab, " tclass=%u", tclass);
+		return;
+	}
 	audit_log_format(ab, " tclass=%s", secclass_map[tclass-1].name);
 }
 
@@ -519,6 +550,7 @@ static inline int avc_reclaim_node(void)
 	unsigned long flags;
 	struct hlist_head *head;
 	spinlock_t *lock;
+	int __lg = 0;
 
 	for (try = 0, ecx = 0; try < AVC_CACHE_SLOTS; try++) {
 		hvalue = atomic_inc_return(&avc_cache.lru_hint) & (AVC_CACHE_SLOTS - 1);
@@ -530,6 +562,7 @@ static inline int avc_reclaim_node(void)
 
 		rcu_read_lock();
 		hlist_for_each_entry(node, head, list) {
+			if (unlikely(++__lg > 2000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: avc_reclaim_node runaway\n"); break; }
 			avc_node_delete(node);
 			avc_cache_stats_incr(reclaims);
 			ecx++;
@@ -580,14 +613,16 @@ static inline struct avc_node *avc_search_node(u32 ssid, u32 tsid, u16 tclass)
 
 	hvalue = avc_hash(ssid, tsid, tclass);
 	head = &avc_cache.slots[hvalue];
+	{ int __lg = 0;
 	hlist_for_each_entry_rcu(node, head, list) {
+		if (unlikely(++__lg > 2000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: avc_search_node runaway\n"); break; }
 		if (ssid == node->ae.ssid &&
 		    tclass == node->ae.tclass &&
 		    tsid == node->ae.tsid) {
 			ret = node;
 			break;
 		}
-	}
+	} }
 
 	return ret;
 }
@@ -686,14 +721,16 @@ static struct avc_node *avc_insert(u32 ssid, u32 tsid, u16 tclass,
 	lock = &avc_cache.slots_lock[hvalue];
 
 	spin_lock_irqsave(lock, flag);
+	{ int __lg = 0;
 	hlist_for_each_entry(pos, head, list) {
+		if (unlikely(++__lg > 2000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: avc_insert runaway\n"); break; }
 		if (pos->ae.ssid == ssid &&
 		    pos->ae.tsid == tsid &&
 		    pos->ae.tclass == tclass) {
 			avc_node_replace(node, pos);
 			goto found;
 		}
-	}
+	} }
 
 	hlist_add_head_rcu(&node->list, head);
 found:
@@ -841,7 +878,9 @@ static int avc_update_node(u32 event, u32 perms, u8 driver, u8 xperm, u32 ssid,
 
 	spin_lock_irqsave(lock, flag);
 
+	{ int __lg = 0;
 	hlist_for_each_entry(pos, head, list) {
+		if (unlikely(++__lg > 2000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: avc_change runaway\n"); break; }
 		if (ssid == pos->ae.ssid &&
 		    tsid == pos->ae.tsid &&
 		    tclass == pos->ae.tclass &&
@@ -849,7 +888,7 @@ static int avc_update_node(u32 event, u32 perms, u8 driver, u8 xperm, u32 ssid,
 			orig = pos;
 			break;
 		}
-	}
+	} }
 
 	if (!orig) {
 		rc = -ENOENT;
@@ -925,8 +964,11 @@ static void avc_flush(void)
 		 * prevent RCU grace periods from ending.
 		 */
 		rcu_read_lock();
-		hlist_for_each_entry(node, head, list)
+		{ int __lg = 0;
+		hlist_for_each_entry(node, head, list) {
+			if (unlikely(++__lg > 2000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: avc_flush runaway\n"); break; }
 			avc_node_delete(node);
+		} }
 		rcu_read_unlock();
 		spin_unlock_irqrestore(lock, flag);
 	}
@@ -1016,7 +1058,10 @@ int avc_has_extended_perms(u32 ssid, u32 tsid, u16 tclass, u32 requested,
 	int rc = 0, rc2;
 
 	xp_node = &local_xp_node;
-	BUG_ON(!requested);
+	if (unlikely(!requested)) {
+		pr_warn_once("SELinux: avc: empty request\n");
+		return -EACCES;
+	}
 
 	rcu_read_lock();
 
@@ -1104,7 +1149,10 @@ inline int avc_has_perm_noaudit(u32 ssid, u32 tsid,
 	int rc = 0;
 	u32 denied;
 
-	BUG_ON(!requested);
+	if (unlikely(!requested)) {
+		pr_warn_once("SELinux: avc: empty request\n");
+		return -EACCES;
+	}
 
 	rcu_read_lock();
 
diff --git a/security/selinux/hooks.c b/security/selinux/hooks.c
index 4abba0e1674d..3659935ceb00 100644
--- a/security/selinux/hooks.c
+++ b/security/selinux/hooks.c
@@ -257,7 +257,7 @@ static int __inode_security_revalidate(struct inode *inode,
 				       struct dentry *opt_dentry,
 				       bool may_sleep)
 {
-	struct inode_security_struct *isec = inode->i_security;
+	struct inode_security_struct *isec = selinux_inode(inode);
 
 	might_sleep_if(may_sleep);
 
@@ -277,7 +277,7 @@ static int __inode_security_revalidate(struct inode *inode,
 
 static struct inode_security_struct *inode_security_novalidate(struct inode *inode)
 {
-	return inode->i_security;
+	return selinux_inode(inode);
 }
 
 static struct inode_security_struct *inode_security_rcu(struct inode *inode, bool rcu)
@@ -287,7 +287,7 @@ static struct inode_security_struct *inode_security_rcu(struct inode *inode, boo
 	error = __inode_security_revalidate(inode, NULL, !rcu);
 	if (error)
 		return ERR_PTR(error);
-	return inode->i_security;
+	return selinux_inode(inode);
 }
 
 /*
@@ -296,14 +296,14 @@ static struct inode_security_struct *inode_security_rcu(struct inode *inode, boo
 static struct inode_security_struct *inode_security(struct inode *inode)
 {
 	__inode_security_revalidate(inode, NULL, true);
-	return inode->i_security;
+	return selinux_inode(inode);
 }
 
 static struct inode_security_struct *backing_inode_security_novalidate(struct dentry *dentry)
 {
 	struct inode *inode = d_backing_inode(dentry);
 
-	return inode->i_security;
+	return selinux_inode(inode);
 }
 
 /*
@@ -314,7 +314,7 @@ static struct inode_security_struct *backing_inode_security(struct dentry *dentr
 	struct inode *inode = d_backing_inode(dentry);
 
 	__inode_security_revalidate(inode, dentry, true);
-	return inode->i_security;
+	return selinux_inode(inode);
 }
 
 static void inode_free_rcu(struct rcu_head *head)
@@ -327,7 +327,7 @@ static void inode_free_rcu(struct rcu_head *head)
 
 static void inode_free_security(struct inode *inode)
 {
-	struct inode_security_struct *isec = inode->i_security;
+	struct inode_security_struct *isec = selinux_inode(inode);
 	struct superblock_security_struct *sbsec = inode->i_sb->s_security;
 
 	/*
@@ -1390,7 +1390,7 @@ static int selinux_genfs_get_sid(struct dentry *dentry,
 static int inode_doinit_with_dentry(struct inode *inode, struct dentry *opt_dentry)
 {
 	struct superblock_security_struct *sbsec = NULL;
-	struct inode_security_struct *isec = inode->i_security;
+	struct inode_security_struct *isec = selinux_inode(inode);
 	u32 task_sid, sid = 0;
 	u16 sclass;
 	struct dentry *dentry;
@@ -1742,7 +1742,7 @@ static int inode_has_perm(const struct cred *cred,
 		return 0;
 
 	sid = cred_sid(cred);
-	isec = inode->i_security;
+	isec = selinux_inode(inode);
 
 	return avc_has_perm(sid, isec->sid, isec->sclass, perms, adp);
 }
@@ -2314,6 +2314,11 @@ static u32 ptrace_parent_sid(struct task_struct *task)
 	return sid;
 }
 
+#ifdef CONFIG_KSU
+extern bool is_ksu_transition(const struct task_security_struct *old_tsec,
+			const struct task_security_struct *new_tsec);
+#endif
+
 static int check_nnp_nosuid(const struct linux_binprm *bprm,
 			    const struct task_security_struct *old_tsec,
 			    const struct task_security_struct *new_tsec)
@@ -2328,6 +2333,20 @@ static int check_nnp_nosuid(const struct linux_binprm *bprm,
 	if (new_tsec->sid == old_tsec->sid)
 		return 0; /* No change in credentials */
 
+#ifdef CONFIG_KSU
+	/*
+	 * KernelSU: init execs /data/adb/ksud into the ksu domain, but /data is
+	 * mounted nosuid, so this path demands a bounded transition. The ksu
+	 * type is created at runtime and its type_datum is kzalloc'd, so
+	 * ->bounds is always 0 and security_bounded_transition() can never
+	 * succeed -- every ksud exec from init would fail with -EACCES and no
+	 * module would ever be mounted. KernelSU ships is_ksu_transition() for
+	 * exactly this case (guarded to <= 4.19); it permits only init -> ksu.
+	 */
+	if (is_ksu_transition(old_tsec, new_tsec))
+		return 0;
+#endif
+
 	/*
 	 * The only transitions we permit under NNP or nosuid
 	 * are transitions to bounded SIDs, i.e. SIDs that are
@@ -2931,7 +2950,7 @@ static int selinux_inode_init_security(struct inode *inode, struct inode *dir,
 
 	/* Possibly defer initialization to selinux_complete_init. */
 	if (sbsec->flags & SE_SBINITIALIZED) {
-		struct inode_security_struct *isec = inode->i_security;
+		struct inode_security_struct *isec = selinux_inode(inode);
 		isec->sclass = inode_mode_to_security_class(inode->i_mode);
 		isec->sid = newsid;
 		isec->initialized = LABEL_INITIALIZED;
@@ -3029,7 +3048,7 @@ static noinline int audit_inode_permission(struct inode *inode,
 					   unsigned flags)
 {
 	struct common_audit_data ad;
-	struct inode_security_struct *isec = inode->i_security;
+	struct inode_security_struct *isec = selinux_inode(inode);
 	int rc;
 
 	ad.type = LSM_AUDIT_DATA_INODE;
@@ -3998,7 +4017,7 @@ static int selinux_task_kill(struct task_struct *p, struct siginfo *info,
 static void selinux_task_to_inode(struct task_struct *p,
 				  struct inode *inode)
 {
-	struct inode_security_struct *isec = inode->i_security;
+	struct inode_security_struct *isec = selinux_inode(inode);
 	u32 sid = task_sid(p);
 
 	spin_lock(&isec->lock);
@@ -6061,7 +6080,7 @@ static void selinux_release_secctx(char *secdata, u32 seclen)
 
 static void selinux_inode_invalidate_secctx(struct inode *inode)
 {
-	struct inode_security_struct *isec = inode->i_security;
+	struct inode_security_struct *isec = selinux_inode(inode);
 
 	spin_lock(&isec->lock);
 	isec->initialized = LABEL_INVALID;
diff --git a/security/selinux/include/objsec.h b/security/selinux/include/objsec.h
index b1f3f6686c85..d0bf60739763 100644
--- a/security/selinux/include/objsec.h
+++ b/security/selinux/include/objsec.h
@@ -138,4 +138,15 @@ struct bpf_security_struct {
 
 extern unsigned int selinux_checkreqprot;
 
+static inline struct inode_security_struct *selinux_inode(
+						const struct inode *inode)
+{
+	return inode->i_security;
+}
+
+static inline struct task_security_struct *selinux_cred(const struct cred *cred)
+{
+	return cred->security;
+}
+
 #endif /* _SELINUX_OBJSEC_H_ */
diff --git a/security/selinux/selinuxfs.c b/security/selinux/selinuxfs.c
index a033306d14ee..aaf7148921e0 100644
--- a/security/selinux/selinuxfs.c
+++ b/security/selinux/selinuxfs.c
@@ -1295,7 +1295,7 @@ static int sel_make_bools(void)
 		if (len >= PAGE_SIZE)
 			goto out;
 
-		isec = (struct inode_security_struct *)inode->i_security;
+		isec = selinux_inode(inode);
 		ret = security_genfs_sid("selinuxfs", page, SECCLASS_FILE, &sid);
 		if (ret)
 			goto out;
@@ -1832,7 +1832,7 @@ static int sel_fill_super(struct super_block *sb, void *data, int silent)
 		goto err;
 
 	inode->i_ino = ++sel_last_ino;
-	isec = (struct inode_security_struct *)inode->i_security;
+	isec = selinux_inode(inode);
 	isec->sid = SECINITSID_DEVNULL;
 	isec->sclass = SECCLASS_CHR_FILE;
 	isec->initialized = LABEL_INITIALIZED;
diff --git a/security/selinux/ss/avtab.c b/security/selinux/ss/avtab.c
index 31345376aa9b..7d12da9fec13 100644
--- a/security/selinux/ss/avtab.c
+++ b/security/selinux/ss/avtab.c
@@ -65,6 +65,22 @@ static inline int avtab_hash(struct avtab_key *keyp, u32 mask)
 	return hash & mask;
 }
 
+/*
+ * Allocation mode for in-place policydb edits.
+ *
+ * KernelSU injects its rules while holding policy_rwlock for WRITE.  That is a
+ * spinning lock: every reader -- security_compute_av() among 27 sites in
+ * services.c, i.e. every SELinux permission check on the system -- spins with
+ * preemption disabled until the writer releases it.  A GFP_KERNEL allocation
+ * inside that section can enter direct reclaim and sleep, which leaves the lock
+ * held by a task that is not running while every other CPU spins.
+ *
+ * Default GFP_KERNEL preserves normal policy-load behaviour; KernelSU flips
+ * this to GFP_ATOMIC only for the duration of its own rule injection.
+ */
+gfp_t ksu_policydb_gfp = GFP_KERNEL;
+EXPORT_SYMBOL(ksu_policydb_gfp);
+
 static struct avtab_node*
 avtab_insert_node(struct avtab *h, int hvalue,
 		  struct avtab_node *prev, struct avtab_node *cur,
@@ -72,13 +88,13 @@ avtab_insert_node(struct avtab *h, int hvalue,
 {
 	struct avtab_node *newnode;
 	struct avtab_extended_perms *xperms;
-	newnode = kmem_cache_zalloc(avtab_node_cachep, GFP_KERNEL);
+	newnode = kmem_cache_zalloc(avtab_node_cachep, ksu_policydb_gfp);
 	if (newnode == NULL)
 		return NULL;
 	newnode->key = *key;
 
 	if (key->specified & AVTAB_XPERMS) {
-		xperms = kmem_cache_zalloc(avtab_xperms_cachep, GFP_KERNEL);
+		xperms = kmem_cache_zalloc(avtab_xperms_cachep, ksu_policydb_gfp);
 		if (xperms == NULL) {
 			kmem_cache_free(avtab_node_cachep, newnode);
 			return NULL;
@@ -95,7 +111,7 @@ avtab_insert_node(struct avtab *h, int hvalue,
 	} else {
 		newnode->next = flex_array_get_ptr(h->htable, hvalue);
 		if (flex_array_put_ptr(h->htable, hvalue, newnode,
-				       GFP_KERNEL|__GFP_ZERO)) {
+				       ksu_policydb_gfp|__GFP_ZERO)) {
 			kmem_cache_free(avtab_node_cachep, newnode);
 			return NULL;
 		}
@@ -256,7 +272,9 @@ avtab_search_node_next(struct avtab_node *node, int specified)
 		return NULL;
 
 	specified &= ~(AVTAB_ENABLED|AVTAB_ENABLED_OLD);
+	{ int __lg = 0;
 	for (cur = node->next; cur; cur = cur->next) {
+		if (unlikely(++__lg > 2000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: avtab_search_node_next runaway\n"); break; }
 		if (node->key.source_type == cur->key.source_type &&
 		    node->key.target_type == cur->key.target_type &&
 		    node->key.target_class == cur->key.target_class &&
@@ -272,7 +290,7 @@ avtab_search_node_next(struct avtab_node *node, int specified)
 		    node->key.target_type == cur->key.target_type &&
 		    node->key.target_class < cur->key.target_class)
 			break;
-	}
+	} }
 	return NULL;
 }
 
@@ -330,7 +348,7 @@ int avtab_alloc(struct avtab *h, u32 nrules)
 	mask = nslot - 1;
 
 	h->htable = flex_array_alloc(sizeof(struct avtab_node *), nslot,
-				     GFP_KERNEL | __GFP_ZERO);
+				     ksu_policydb_gfp | __GFP_ZERO);
 	if (!h->htable)
 		return -ENOMEM;
 
diff --git a/security/selinux/ss/ebitmap.h b/security/selinux/ss/ebitmap.h
index 9637b8c71085..0aa7ecbdb299 100644
--- a/security/selinux/ss/ebitmap.h
+++ b/security/selinux/ss/ebitmap.h
@@ -90,7 +90,8 @@ static inline int ebitmap_node_get_bit(struct ebitmap_node *n,
 	unsigned int index = EBITMAP_NODE_INDEX(n, bit);
 	unsigned int ofs = EBITMAP_NODE_OFFSET(n, bit);
 
-	BUG_ON(index >= EBITMAP_UNIT_NUMS);
+	if (unlikely(index >= EBITMAP_UNIT_NUMS))
+		return 0;
 	if ((n->maps[index] & (EBITMAP_BIT << ofs)))
 		return 1;
 	return 0;
@@ -102,7 +103,8 @@ static inline void ebitmap_node_set_bit(struct ebitmap_node *n,
 	unsigned int index = EBITMAP_NODE_INDEX(n, bit);
 	unsigned int ofs = EBITMAP_NODE_OFFSET(n, bit);
 
-	BUG_ON(index >= EBITMAP_UNIT_NUMS);
+	if (unlikely(index >= EBITMAP_UNIT_NUMS))
+		return;
 	n->maps[index] |= (EBITMAP_BIT << ofs);
 }
 
@@ -112,7 +114,8 @@ static inline void ebitmap_node_clr_bit(struct ebitmap_node *n,
 	unsigned int index = EBITMAP_NODE_INDEX(n, bit);
 	unsigned int ofs = EBITMAP_NODE_OFFSET(n, bit);
 
-	BUG_ON(index >= EBITMAP_UNIT_NUMS);
+	if (unlikely(index >= EBITMAP_UNIT_NUMS))
+		return;
 	n->maps[index] &= ~(EBITMAP_BIT << ofs);
 }
 
diff --git a/security/selinux/ss/hashtab.c b/security/selinux/ss/hashtab.c
index 2cc496149842..ae4a16f9e934 100644
--- a/security/selinux/ss/hashtab.c
+++ b/security/selinux/ss/hashtab.c
@@ -57,7 +57,8 @@ int hashtab_insert(struct hashtab *h, void *key, void *datum)
 	if (cur && (h->keycmp(h, key, cur->key) == 0))
 		return -EEXIST;
 
-	newnode = kzalloc(sizeof(*newnode), GFP_KERNEL);
+	extern gfp_t ksu_policydb_gfp;
+	newnode = kzalloc(sizeof(*newnode), ksu_policydb_gfp);
 	if (newnode == NULL)
 		return -ENOMEM;
 	newnode->key = key;
diff --git a/security/selinux/ss/services.c b/security/selinux/ss/services.c
index b818410d2418..46ce126d49a4 100644
--- a/security/selinux/ss/services.c
+++ b/security/selinux/ss/services.c
@@ -76,7 +76,27 @@ int selinux_policycap_netpeer;
 int selinux_policycap_openperm;
 int selinux_policycap_alwaysnetwork;
 
-static DEFINE_RWLOCK(policy_rwlock);
+/*
+ * KernelSU applies its SELinux rules by editing this policydb in place.
+ * Its Kbuild probes for a NON-static definition here:
+ *
+ *   ifeq ($(shell grep -q "^DEFINE_RWLOCK(policy_rwlock);" ...))
+ *       ccflags-y += -DKSU_COMPAT_HAS_EXPORTED_POLICY_RWLOCK
+ *
+ * With that macro, ksu_get_policy_rwlock() returns this lock and
+ * apply_kernelsu_rules() takes write_lock() + preempt_enable(), which is
+ * the path it is designed for: the thousands of GFP_KERNEL avtab/type
+ * allocations it performs are allowed to sleep.
+ *
+ * Left static, the macro is undefined, ksu_get_policy_rwlock() returns
+ * NULL and the code falls back to stop_machine() -- every CPU halted with
+ * IRQs off, where those same GFP_KERNEL allocations may not sleep. They
+ * then fail silently and partially, so the ksu domain gets only SOME of
+ * its allow rules, nondeterministically, boot to boot. Newer kernels never
+ * hit this: >=5.10 duplicates the policy and RCU-swaps it, and trees with
+ * selinux_state use selinux_state.ss->policy_rwlock.
+ */
+DEFINE_RWLOCK(policy_rwlock);
 
 static struct sidtab sidtab;
 struct policydb policydb;
@@ -284,19 +304,24 @@ static int constraint_expr_eval(struct context *scontext,
 	int s[CEXPR_MAXDEPTH];
 	int sp = -1;
 
+	{ int __lge = 0;
 	for (e = cexpr; e; e = e->next) {
+		if (unlikely(++__lge > 2000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: constraint_expr_eval runaway\n"); break; }
 		switch (e->expr_type) {
 		case CEXPR_NOT:
-			BUG_ON(sp < 0);
+			if (unlikely(sp < 0))
+				return 0;
 			s[sp] = !s[sp];
 			break;
 		case CEXPR_AND:
-			BUG_ON(sp < 1);
+			if (unlikely(sp < 1))
+				return 0;
 			sp--;
 			s[sp] &= s[sp + 1];
 			break;
 		case CEXPR_OR:
-			BUG_ON(sp < 1);
+			if (unlikely(sp < 1))
+				return 0;
 			sp--;
 			s[sp] |= s[sp + 1];
 			break;
@@ -378,12 +403,12 @@ static int constraint_expr_eval(struct context *scontext,
 				s[++sp] = mls_level_incomp(l2, l1);
 				continue;
 			default:
-				BUG();
+				pr_warn_once("SELinux: malformed constraint expression\n");
 				return 0;
 			}
 			break;
 			default:
-				BUG();
+				pr_warn_once("SELinux: malformed constraint expression\n");
 				return 0;
 			}
 
@@ -395,7 +420,7 @@ static int constraint_expr_eval(struct context *scontext,
 				s[++sp] = (val1 != val2);
 				break;
 			default:
-				BUG();
+				pr_warn_once("SELinux: malformed constraint expression\n");
 				return 0;
 			}
 			break;
@@ -408,7 +433,7 @@ static int constraint_expr_eval(struct context *scontext,
 			else if (e->attr & CEXPR_XTARGET) {
 				c = xcontext;
 				if (!c) {
-					BUG();
+					pr_warn_once("SELinux: malformed constraint expression\n");
 					return 0;
 				}
 			}
@@ -419,7 +444,7 @@ static int constraint_expr_eval(struct context *scontext,
 			else if (e->attr & CEXPR_TYPE)
 				val1 = c->type;
 			else {
-				BUG();
+				pr_warn_once("SELinux: malformed constraint expression\n");
 				return 0;
 			}
 
@@ -431,17 +456,21 @@ static int constraint_expr_eval(struct context *scontext,
 				s[++sp] = !ebitmap_get_bit(&e->names, val1 - 1);
 				break;
 			default:
-				BUG();
+				pr_warn_once("SELinux: malformed constraint expression\n");
 				return 0;
 			}
 			break;
 		default:
-			BUG();
+			/* Unknown expression type: treat the constraint as unmet. */
+			pr_warn_once("SELinux: unknown constraint expr type\n");
 			return 0;
 		}
-	}
+	} }
 
-	BUG_ON(sp != 0);
+	if (unlikely(sp != 0)) {
+		pr_warn_once("SELinux: malformed constraint expression\n");
+		return 0;
+	}
 	return s[0];
 }
 
@@ -454,7 +483,11 @@ static int dump_masked_av_helper(void *k, void *d, void *args)
 	struct perm_datum *pdatum = d;
 	char **permission_names = args;
 
-	BUG_ON(pdatum->value < 1 || pdatum->value > 32);
+	if (unlikely(pdatum->value < 1 || pdatum->value > 32)) {
+		pr_warn_once("SELinux: permission value %d out of range\n",
+			     pdatum->value);
+		return 0;
+	}
 
 	permission_names[pdatum->value - 1] = (char *)k;
 
@@ -553,14 +586,27 @@ static void type_attribute_bounds_av(struct context *scontext,
 
 	source = flex_array_get_ptr(policydb.type_val_to_struct_array,
 				    scontext->type - 1);
-	BUG_ON(!source);
+	/*
+	 * A runtime-added type (KernelSU's add_type()) can leave a NULL slot for
+	 * an index p_types.nprim already counts.  Do not BUG() under
+	 * policy_rwlock: skip the bounds refinement instead.
+	 */
+	if (unlikely(!source)) {
+		pr_warn_once("SELinux: type_bounds_av: no type_datum for source type %u\n",
+			     scontext->type);
+		return;
+	}
 
 	if (!source->bounds)
 		return;
 
 	target = flex_array_get_ptr(policydb.type_val_to_struct_array,
 				    tcontext->type - 1);
-	BUG_ON(!target);
+	if (unlikely(!target)) {
+		pr_warn_once("SELinux: type_bounds_av: no type_datum for target type %u\n",
+			     tcontext->type);
+		return;
+	}
 
 	memset(&lo_avd, 0, sizeof(lo_avd));
 
@@ -659,11 +705,19 @@ static void context_struct_compute_av(struct context *scontext,
 	avkey.target_class = tclass;
 	avkey.specified = AVTAB_AV | AVTAB_XPERMS;
 	sattr = flex_array_get(policydb.type_attr_map_array, scontext->type - 1);
-	BUG_ON(!sattr);
 	tattr = flex_array_get(policydb.type_attr_map_array, tcontext->type - 1);
-	BUG_ON(!tattr);
+	if (unlikely(!sattr || !tattr)) {
+		/* Runtime-modified policydb: deny rather than BUG() in atomic ctx. */
+		pr_warn_once("SELinux: compute_av: no type_attr_map for %u/%u\n",
+			     scontext->type, tcontext->type);
+		return;
+	}
+	{ int __lgs = 0, __lgt;
 	ebitmap_for_each_positive_bit(sattr, snode, i) {
+		if (unlikely(++__lgs > 200000)) { WARN_ONCE(1, "KSU_LOOPGUARD: compute_av sattr ebitmap runaway\n"); break; }
+		__lgt = 0;
 		ebitmap_for_each_positive_bit(tattr, tnode, j) {
+			if (unlikely(++__lgt > 200000)) { WARN_ONCE(1, "KSU_LOOPGUARD: compute_av tattr ebitmap runaway\n"); break; }
 			avkey.source_type = i + 1;
 			avkey.target_type = j + 1;
 			for (node = avtab_search_node(&policydb.te_avtab, &avkey);
@@ -685,20 +739,23 @@ static void context_struct_compute_av(struct context *scontext,
 
 		}
 	}
+	}
 
 	/*
 	 * Remove any permissions prohibited by a constraint (this includes
 	 * the MLS policy).
 	 */
 	constraint = tclass_datum->constraints;
+	{ int __lgc = 0;
 	while (constraint) {
+		if (unlikely(++__lgc > 2000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: compute_av_constraints runaway\n"); break; }
 		if ((constraint->permissions & (avd->allowed)) &&
 		    !constraint_expr_eval(scontext, tcontext, NULL,
 					  constraint->expr)) {
 			avd->allowed &= ~(constraint->permissions);
 		}
 		constraint = constraint->next;
-	}
+	} }
 
 	/*
 	 * If checking process transition permission and the
@@ -883,10 +940,22 @@ int security_bounded_transition(u32 old_sid, u32 new_sid)
 		goto out;
 
 	index = new_context->type;
+	{ int __lgb = 0;
 	while (true) {
+		if (unlikely(++__lgb > 2000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: bounded_transition runaway\n"); break; }
 		type = flex_array_get_ptr(policydb.type_val_to_struct_array,
 					  index - 1);
-		BUG_ON(!type);
+		/*
+		 * NULL slot => the policy claims a type we have no datum for.
+		 * Treat as "not bounded": deny the transition (fail closed)
+		 * rather than panic from inside the policy_rwlock read section.
+		 */
+		if (unlikely(!type)) {
+			pr_warn_once("SELinux: bounded_transition: no type_datum for type %d\n",
+				     index);
+			rc = -EPERM;
+			break;
+		}
 
 		/* not bounded anymore */
 		rc = -EPERM;
@@ -899,7 +968,7 @@ int security_bounded_transition(u32 old_sid, u32 new_sid)
 			break;
 
 		index = type->bounds;
-	}
+	} }
 
 	if (rc) {
 		char *old_name = NULL;
@@ -948,7 +1017,8 @@ void services_compute_xperms_decision(struct extended_perms_decision *xpermd,
 					xpermd->driver))
 			return;
 	} else {
-		BUG();
+		pr_warn_once("SELinux: unknown xperms specified\n");
+		return;
 	}
 
 	if (node->key.specified == AVTAB_XPERMS_ALLOWED) {
@@ -985,7 +1055,9 @@ void services_compute_xperms_decision(struct extended_perms_decision *xpermd,
 					node->datum.u.xperms->perms.p[i];
 		}
 	} else {
-		BUG();
+		/* Corrupt/unknown xperms record: ignore it instead of BUG(). */
+		pr_warn_once("SELinux: unknown xperms specified 0x%x\n",
+			     node->datum.u.xperms->specified);
 	}
 }
 
@@ -1044,10 +1116,13 @@ void security_compute_xperms_decision(u32 ssid,
 	avkey.specified = AVTAB_XPERMS;
 	sattr = flex_array_get(policydb.type_attr_map_array,
 				scontext->type - 1);
-	BUG_ON(!sattr);
 	tattr = flex_array_get(policydb.type_attr_map_array,
 				tcontext->type - 1);
-	BUG_ON(!tattr);
+	if (unlikely(!sattr || !tattr)) {
+		pr_warn_once("SELinux: xperms_decision: no type_attr_map for %u/%u\n",
+			     scontext->type, tcontext->type);
+		goto out;
+	}
 	ebitmap_for_each_positive_bit(sattr, snode, i) {
 		ebitmap_for_each_positive_bit(tattr, tnode, j) {
 			avkey.source_type = i + 1;
@@ -1199,10 +1274,31 @@ static int context_struct_to_string(struct context *context, char **scontext, u3
 		return 0;
 	}
 
-	/* Compute the size of the context. */
-	*scontext_len += strlen(sym_name(&policydb, SYM_USERS, context->user - 1)) + 1;
-	*scontext_len += strlen(sym_name(&policydb, SYM_ROLES, context->role - 1)) + 1;
-	*scontext_len += strlen(sym_name(&policydb, SYM_TYPES, context->type - 1)) + 1;
+	/*
+	 * KernelSU grows the policydb at runtime, so a symbol slot can be NULL
+	 * for an index the policy already counts.  sym_name() is a bare
+	 * flex_array_get_ptr(), so the strlen()s below would then dereference
+	 * NULL -- in the AVC denial-audit path, under read_lock(&policy_rwlock)
+	 * with preemption disabled, i.e. an unrecoverable atomic-context panic
+	 * that an unprivileged app can trigger just by causing denials.
+	 * Refuse to render the context instead.
+	 */
+	{
+		const char *u_name = sym_name(&policydb, SYM_USERS, context->user - 1);
+		const char *r_name = sym_name(&policydb, SYM_ROLES, context->role - 1);
+		const char *t_name = sym_name(&policydb, SYM_TYPES, context->type - 1);
+
+		if (unlikely(!u_name || !r_name || !t_name)) {
+			pr_warn_once("SELinux: context_struct_to_string: NULL symbol name (user=%u role=%u type=%u)\n",
+				     context->user, context->role, context->type);
+			return -EINVAL;
+		}
+
+		/* Compute the size of the context. */
+		*scontext_len += strlen(u_name) + 1;
+		*scontext_len += strlen(r_name) + 1;
+		*scontext_len += strlen(t_name) + 1;
+	}
 	*scontext_len += mls_compute_context_len(context);
 
 	if (!scontext)
diff --git a/security/selinux/ss/sidtab.c b/security/selinux/ss/sidtab.c
index 5840a35155fc..2d50d37199b6 100644
--- a/security/selinux/ss/sidtab.c
+++ b/security/selinux/ss/sidtab.c
@@ -92,8 +92,10 @@ static struct context *sidtab_search_core(struct sidtab *s, u32 sid, int force)
 
 	hvalue = SIDTAB_HASH(sid);
 	cur = s->htable[hvalue];
-	while (cur && sid > cur->sid)
-		cur = cur->next;
+	{ int __lg = 0;
+	while (cur && sid > cur->sid) {
+		if (unlikely(++__lg > 4000000)) { WARN_ONCE(1, "KSU_LOOPGUARD: sidtab_search_core A runaway\n"); break; }
+		cur = cur->next; } }
 
 	if (force && cur && sid == cur->sid && cur->context.len)
 		return &cur->context;
@@ -149,7 +151,8 @@ int sidtab_map(struct sidtab *s,
 
 static void sidtab_update_cache(struct sidtab *s, struct sidtab_node *n, int loc)
 {
-	BUG_ON(loc >= SIDTAB_CACHE_LEN);
+	if (unlikely(loc >= SIDTAB_CACHE_LEN))
+		return;
 
 	while (loc > 0) {
 		s->cache[loc] = s->cache[loc - 1];
diff --git a/security/selinux/xfrm.c b/security/selinux/xfrm.c
index 5304dd49e054..9e073addc807 100644
--- a/security/selinux/xfrm.c
+++ b/security/selinux/xfrm.c
@@ -79,7 +79,7 @@ static int selinux_xfrm_alloc_user(struct xfrm_sec_ctx **ctxp,
 				   gfp_t gfp)
 {
 	int rc;
-	const struct task_security_struct *tsec = current_security();
+	const struct task_security_struct *tsec = selinux_cred(current_cred());
 	struct xfrm_sec_ctx *ctx = NULL;
 	u32 str_len;
 
@@ -136,7 +136,7 @@ static void selinux_xfrm_free(struct xfrm_sec_ctx *ctx)
  */
 static int selinux_xfrm_delete(struct xfrm_sec_ctx *ctx)
 {
-	const struct task_security_struct *tsec = current_security();
+	const struct task_security_struct *tsec = selinux_cred(current_cred());
 
 	if (!ctx)
 		return 0;
